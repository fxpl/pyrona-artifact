# UV for virtual environments

This artifact uses UV to create virtual environments and
install required packages.

This folder contains two project folders, one for python3.14
used for the website and creating plots since these need wheels
internally.

The snapshot folder contains the dependencies that should be
installed in the virtual environments for the snapshots. The same
project and locked file will be used to ensure similar behaviour.
